---
import { withBase } from "../lib/paths";
---

<section class="hero">
  <div class="container">
    <p class="eyebrow">宋式 AI 设计系统</p>
    <h1 class="display-title">AI 不懂得何时停止。</h1>
    <p class="hero-statement">同样的内容，SongStyle 让设计从“不断添加”转向“准确表达”。</p>
    <p class="body-copy">
      一套帮助设计师、开发者与 AI 判断什么应该存在、如何建立秩序，以及表达应在何处停止的现代设计语言。
      它不是古风装饰，也不是把界面变得空旷。
    </p>
    <div class="hero-actions">
      <a class="primary-link" href="#evidence">查看公平对比</a>
      <a class="quiet-link" href={withBase("/prompts/")}>开始使用 Prompts <span aria-hidden="true">→</span></a>
    </div>
  </div>
</section>

<style>
  .hero {
    display: grid;
    min-height: min(78vh, 760px);
    align-items: center;
    padding-block: var(--song-space-section-spacious);
  }

  .hero-statement {
    max-width: 28ch;
    margin: var(--song-space-scale-8) 0 var(--song-space-scale-6);
    color: var(--song-color-text-secondary);
    font-family: var(--song-typography-family-display);
    font-size: var(--song-typography-size-title-small);
    line-height: var(--song-typography-line-height-body);
  }

  .body-copy {
    margin: 0 0 var(--song-space-scale-8);
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--song-space-scale-6);
  }

  .primary-link {
    padding: var(--song-space-scale-3) var(--song-space-scale-5);
    border-radius: var(--song-shape-radius-control);
    background: var(--song-color-text-primary);
    color: var(--song-color-background-canvas);
    font-weight: var(--song-typography-weight-medium);
    text-decoration: none;
  }
</style>
